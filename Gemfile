source "https://rubygems.org"

# This gem includes the standard GitHub Pages environment (Jekyll + default themes)
gem "github-pages", group: :jekyll_plugins

# These gems specifically enable the features we discussed
gem "jekyll-redirect-from"
gem "jekyll-sitemap"