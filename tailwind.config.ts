import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: '#ffe70b',
        secondary: '#ff8c00',
      },
    },
  },
  plugins: [],
};

export default config;
